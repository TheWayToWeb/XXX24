// Подключаем функцию createContext из библиотеки React для создания контекста
import { createContext } from 'react';

// Создаем контекст производства
export const ProductionContext = createContext(null);